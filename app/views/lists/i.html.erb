<% @lists.each do |list| %>
<div class="row  mt-4 clearfix">
  <div class="col-8 col-sm-8 col-md-9 col-lg-9">
    <h5 class="mt-0 mb-1">
      <a href="/lists/<%= list.id %>",
      class="nav-link"><%= list.title %></a>
    </h5>
    <a href="/lists/<%= list.id %>",
    class="nav-link"><%= list.name %></a>
  <strong class="block font-medium mb-1">Art:</strong>
    <%= list.art.name %>
    <br/>
    <%= truncate(strip_tags(list.body.to_s), length:180) %>
  </div>
  <div class="col-4 col-sm-4 col-md-3 col-lg-3 clearfix">
    <% if list.image.attached? %>
      <%= link_to image_tag(list.image,
      style:"width:270px; heigh:auto"), list.image %>
    <% end %>
  </div>
</div>
<% end %>
<hr />

@pagy.count.nil? || .eql? equal? <=> >=
 && h.status == "?query"
h.ab_template.AB_filterParent.nil? || h.ab_template.AB_filterParent == 0
@pagy.count > @pagy.items
<% if "?query" || "?quer" %>
== 0 && h.status != 'geo_enabled'
<% if "?query" %>
<% else @pagy.count > @pagy.items %>
<% if !(@pagy.count > @pagy.items != "?query") %>
<% if @pagy.count > @pagy.items %>
<div class="mt-10">
  <%== pagy_bootstrap_nav(@pagy) %>
</div>
<br />
<% end %>
<div id="lists" class="min-w-full">
  <%= render @lists %>
</div>
<%= render @lists %>
